<!DOCTYPE html>

<html lang="en" class="no-js">

<head>
    <!-- meta character set -->
    <meta charset="utf-8">
    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Social Sense Results</title>
    <!-- Meta Description -->
    <meta name="description" content="Blue One Page Creative HTML5 Template">
    <meta name="keywords" content="one page, single page, onepage, responsive, parallax, creative, business, 
        html5, css3, css3 animation, mental, health, social media, facebook, instagram, reddit, discord, YouTube">
    <meta name="author" content="Tyler Sheffield, Allan Foote, Mikayla Randall, Rai Martin">

    <!-- Mobile Specific Meta -->
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- theme meta -->
    <meta name="theme-name" content="blue-pro" />

    <!-- CSS
        ================================================== -->

    <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Open+Sans:400,300,700">
    <link rel="shortcut icon" type="image/x-icon" href="/images/icon.png" />
    <!-- Fontawesome Icon font -->
    <link rel="stylesheet" href="plugins/font-awesome.min.css">
    <!-- Ion Icons Font -->
    <link rel="stylesheet" href="plugins/ionicons.min.css">
    <!-- bootstrap.min -->
    <link rel="stylesheet" href="plugins/jquery.fancybox.css">
    <!-- bootstrap.min -->
    <link rel="stylesheet" href="plugins/bootstrap/bootstrap.min.css">
    <!-- bootstrap.min -->
    <link rel="stylesheet" href="plugins/owl.carousel.css">
    <!-- bootstrap.min -->
    <link rel="stylesheet" href="plugins/slit-slider.css">
    <!-- bootstrap.min -->
    <link rel="stylesheet" href="plugins/animate.css">
    <!-- Main Stylesheet -->
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/response.css">
    <link rel="stylesheet" href="css/results.css">

    <!-- Modernizer Script for old Browsers -->
    <script src="plugins/modernizr-2.6.2.min.js"></script>
</head>

<body id="body">

    <!-- preloader -->
    <!-- <div id="preloader">
                <div class="loder-box">
                    <div class="battery"></div>
                </div>
        </div> -->
    <!-- end preloader -->

    <!-- Fixed Navigation ==================================== -->
    <header id="navigation" class="fixed-top animated-header">
        <div class="container-fluid">
            <nav class="navbar navbar-expand-lg navbar-dark">
                <a class="navbar-brand" href="/">
                    <img class="img-fluid" src="images/logo.png" alt="">
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse"
                    data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ml-auto text-center" id="nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/survey">Take The Survey</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">Dashboard</a>
                        </li>
                        <% if (!user) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/login" id="login">Login</a>
                            </li>
                            <% } else if (user && user.admin) { %>
                                <!-- Show this if user has admin permissions -->
                                <li class="nav-item">
                                    <a class="nav-link" href="/results">Survey Results</a>
                                </li>
                        
                                <li class="nav-item">
                                    <a class="nav-link" href="/accounts" id="accounts">View Accounts</a>
                                </li>
                        
                                <li class="nav-item">
                                    <a class="nav-link" href="/logout">Logout</a>
                                </li>
                        
                                <% } else { %>
                                    <!-- Show this for general logged-in users -->
                                    <li class="nav-item">
                                        <a class="nav-link" href="/results">Survey Results</a>
                                    </li>
                        
                                    <li class="nav-item">
                                        <a class="nav-link" href="/account" id="account">My Account</a>
                                    </li>
                        
                                    <li class="nav-item">
                                        <a class="nav-link" href="/logout">Logout</a>
                                    </li>
                        
                                    <% } %>-


                    </ul>
                </div>
            </nav>
        </div>
    </header>
    <div class="buffer" id="buffer" style="height: 116px;"></div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Function to get unique values from a column
            function getUniqueValues(columnIndex) {
                var uniqueValues = new Set();
                var rows = document.getElementById("surveyResultsTable").getElementsByTagName("tr");

                for (var i = 0; i < rows.length; i++) {
                    var cell = rows[i].getElementsByTagName("td")[columnIndex];
                    var cellText = cell.innerText.toLowerCase().trim();
                    uniqueValues.add(cellText);
                }

                return Array.from(uniqueValues);
            }

            // Function to update dropdown options
            function updateDropdownOptions(dropdownId, uniqueValues) {
                var dropdown = document.getElementById(dropdownId);
                dropdown.innerHTML = '<option value="all">All</option>'; // Reset dropdown

                uniqueValues.forEach(function (value) {
                    var option = document.createElement("option");
                    option.value = value;
                    option.text = value;
                    dropdown.add(option);
                });
            }

            // Populate dropdowns on page load
            var dropdowns = document.querySelectorAll('.filter-dropdown');

            dropdowns.forEach(function (dropdown) {
                var columnIndex; // Set this to the appropriate column index for each dropdown
                var uniqueValues = getUniqueValues(columnIndex);
                updateDropdownOptions(dropdown.id, uniqueValues);
            });

            // Add logic for saving changes here
            // ...
        });
    </script>

    <main class="site-content" role="main">
        <section class="service-2 section results-page">
            <div class="search-bar">
                <input type="text" id="searchInput" placeholder="Search...">
                <button onclick="search()">Search</button>
            </div>
            <table class="table-style">
                <thead>
                    <tr>
                        <th>Survey Number</th>
                        <th>Time Stamp
                            <select id="monthFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                               
                                <!-- Add more month options as needed -->
                            </select>
                            <select id="dayFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                                <!-- Add more day options as needed -->
                            </select>
                            <select id="yearFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                                <!-- Add more year options as needed -->
                            </select>
                        </th>
                        <th>Gender
                            <select id="genderFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                            </select>
                        </th>
                        <th>Age</th>
                        <th>Location
                            <select id="locationFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                            </select>
                        </th>
                        <th>Relationship
                            <select id="relationshipFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                            </select>
                        </th>
                        <th>Occupation
                            <select id="occupationFilter" class="filter-dropdown" onchange="filterResults()">
                                <option value="all">All</option>
                            </select>
                        </th>
                        <th>See More</th>
                    </tr>
                </thead>
                <tbody id="surveyResultsTable">
                    <% for (let iCount=0; iCount < surveyresults.length; iCount++) { %>
                        <tr>
                            <td>
                                <%= surveyresults[iCount].survey_id %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].formatted_time_stamp %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].gender_description %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].age %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].location %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].relationship_status_description %>
                            </td>
                            <td>
                                <%= surveyresults[iCount].occupation_description %>
                            </td>
                            <td>
                                <a class="see-more" href="/choosesurvey/<%= surveyresults[iCount].survey_id %>">See
                                    More</a>
                            </td>
                        </tr>
                        <% } %>
                </tbody>
            </table>
        </section>
    </main>
    <!-- Twitter Bootstrap -->
    <script src="plugins/bootstrap/bootstrap.min.js"></script>
    <!-- Single Page Nav -->
    <script src="plugins/jquery.singlePageNav.min.js"></script>

    <!-- onscroll animation -->
    <script src="plugins/wow.min.js"></script>
    <!-- Custom Functions -->
    <script src="js/main.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var editButtons = document.querySelectorAll('.edit-btn');

            editButtons.forEach(function (button) {
                button.addEventListener('click', function () {
                    var userId = this.getAttribute('data-survey-id');
                    var editForm = document.getElementById('edit-form-' + userId);

                    // Toggle the display of the edit form
                    if (editForm.style.display === 'none') {
                        editForm.style.display = 'table-row';
                    } else {
                        editForm.style.display = 'none';
                    }
                });
            });

            // Add logic for saving changes here
            // ...
        });
    </script>
</body>

</html>
